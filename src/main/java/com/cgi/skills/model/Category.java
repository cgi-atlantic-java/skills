package com.cgi.skills.model;

import javax.persistence.Entity;

/**
 * Skill category (semantics depend on your skill domain)
 */
@Entity
public class Category extends NamedIdHolder {

    // no further members at this time !
}
